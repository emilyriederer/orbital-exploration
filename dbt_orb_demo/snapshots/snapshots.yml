snapshots:
  - name: pred_churn_snapshot
    relation: ref('pred_churn')
    config:
      unique_key: customer_id
      strategy: check
      check_cols: [pred]
      dbt_valid_to_current: 'make_date(9999,12,31)'